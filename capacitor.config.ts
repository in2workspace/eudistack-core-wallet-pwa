import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.eudistack.wallet',
  appName: 'WalletIN2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
